# 模版进阶
## 非类型模版参数
//#define N 10，我们以前用宏

template<size_t N = 10, bool flag = false>
class Stack
{
private:
	int _a[N];
	int _top;
};
//int main()
//{
//	Stack<> s0;//C++20支持stack s0;
//	Stack<5> s1;
//	Stack<10> s2;//这里可以根据需要传入N的值
//
//	return 0;
//}
但是，只支持整形！！
double（c++20）
## 应用：array
静态数组：int arr1[];
array<int 10> arr2;
两个检查的区别
//	// 越界检查的问题
//	// 静态数组，抽查
//	// 越界读不检查，越界写抽查
与vector的区别：一个在栈一个在堆；
	// 栈
//	array<int, 10> a1;
//	array<int, 100> a2;
//
//	int a3[10];
//	// 越界检查的问题
//	// 静态数组，抽查
//	// 越界读不检查，越界写抽查
//	cout << a3[10] << endl;
//	//a3[10] = 10;
//	//a3[11] = 10;
//	a3[12] = 10;
//	a3[20] = 10;
//
//	// array越界读写都可以检查
//	//a1[10];
//	//a1[12] = 10;
//
//	// 堆
//	vector<int> v(100, 1);
//	cout << sizeof(a2) << endl;
//	cout << sizeof(v) << endl;
//
## 模版的特化
## 类模版的特化
全特化
半特化
## 模版的编译链接